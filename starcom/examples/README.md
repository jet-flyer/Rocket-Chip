# Examples (`examples/`)

Loopback and tiny ground-station demos after codecs exist. Not Rocket-Chip firmware.

Host dissect / teaching CLI is a **sidetrack** (Starcom `AGENT_WHITEBOARD.md`), not mainline IVP. Input is a telem source or the line, not typed junk as the whole package.
