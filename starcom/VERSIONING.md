# Starcom versioning

No tagged release yet. When one exists: SemVer, supported API = `include/starcom/` only.

Locked for the core: C++20, public alias `Starcom::starcom`, namespace `starcom::ccsds`.

Intended policy (historical): `docs/research/library_craft_claude.md` §4.5.
