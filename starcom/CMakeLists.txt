# Starcom — placeholder CMake root
#
# Not wired into Rocket-Chip's top-level CMakeLists.txt yet.
# Phase 0 goal: static library target, host ctest, install/export stubs.
# See research/library_craft_claude.md §7 for intended layout.

cmake_minimum_required(VERSION 3.20)
project(starcom LANGUAGES CXX)

message(STATUS "starcom: scaffold only — no targets defined yet")