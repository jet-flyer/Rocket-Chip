# Core implementation (`src/ccsds/`)

**Placeholder.** Private implementation + internal headers for the sans-I/O protocol core (USLP, COP-1, COP-P, PLTU, codecs). No Rocket-Chip or hardware includes.