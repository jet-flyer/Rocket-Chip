# RP2350 adapters

Generic SPI/GPIO radio port (`BusOps` + `radio_bus_shift_tx` / `radio_bus_shift_rx`). Host tests use a fake bus. Board pins, SX1276 / RFM types, and AO stay in the consumer. Not the portable core. No 211.1 claim. PIO is increment 17.
