# W-5 include/consumer + W-2 concurrency 3-question

**Date:** 2026-08-20 (first sitting) + **2026-08-21 close**. W-5 keep/fold **recommendations** only — not executed as code. Do not couple keep/fold across “same class” thin headers.

**Phase 1 status:** closed 2026-08-21. Three-number table and `g_sensorPhaseDone` confirmation are in “Phase 1 close” below. WB W-5/W-2 rows erased the same sitting.

Grep population: `src/` + `include/` + `test/` (authored). `#include` of the basename. Self-include of the `.cpp` next to its `.h` is counted.

---

## W-5 — direct includes (earn-rent paths + WB seeds)

| Path | Direct includes | Notes (facts, not verdicts) |
|------|----------------:|-----------------------------|
| `include/rocketchip/job.h` | 9 | Also pulled by `config.h` → large **indirect** fan-in |
| `include/rocketchip/job_capabilities.h` | 1 | `health_monitor.cpp` only |
| `job_vehicle.h` / `job_station.h` / `job_relay.h` | 1 each | Selector-only via `job.h` |
| `include/rocketchip/led_patterns.h` | 6 | + `test/test_notify.cpp` |
| `include/rocketchip/prearm_fail_ticks.h` | 2 | `ao_notify.cpp` + host test |
| `src/math/vec3.h` | 16 | Real math API |
| `src/math/mat.h` | 6 | Name-vagueness is a different question than fan-in |
| `src/drivers/rfm95w.h` | 4 | |
| `src/drivers/ws2812_status.h` | 5 | |
| `src/fusion/eskf_brake.h` | 0 | No header include. Brake is a **TU** (`eskf_brake.cpp`); `eskf_runner.cpp` comment only |
| `src/calibration/calibration_data.h` | 5 | |
| `src/calibration/calibration_manager.h` | 9 | |
| `src/calibration/calibration_storage.h` | 4 | |
| `src/flight_director/flight_director.h` | 22 | Hub |
| `src/flight_director/action_executor.h` | 5 | |
| `src/flight_director/guard_combinator.h` | 3 | |
| `src/flight_director/guard_functions.h` | 3 | |
| `src/logging/flight_table.h` | 8 | |
| `src/logging/data_convert.h` | 4 | |
| `src/logging/radio_config_storage.h` | 3 | |
| `src/logging/crc32.h` | 5 | |
| `src/diag/diag_stats.h` | 3 | `src/diag/` holds only this pair |
| `src/notify/notify_backend_audio.cpp` | 0 | Stub TU; no `#include` of the cpp |
| `src/station/station_idle_tick.h` | 2 | `src/station/` holds only this pair |
| `src/safety/anomalous_boot.h` | 3 | |
| `src/safety/flight_in_progress.cpp` | 0 | Sentinel TU, not a header |
| `src/safety/core1_i2c_pause.h` | 3 | |
| `src/core1/sensor_core1.h` | 9 | `src/core1/` holds only this pair |
| `src/active_objects/ao_radio.h` | 9 | |
| `src/shared_state.cpp` / `src/main.cpp` | 0 | TUs, not headers |
| `include/rocketchip/radio_config_table.h` | 4 | WB seed |
| `include/rocketchip/sensor_snapshot.h` | 1 | **Only** `test/test_data_model.cpp` |

Full including-file lists: `build/w5_includes.py` output from this sitting (script not shipped).

### W-5 symbol consumers (thin / disputed)

| Path | Symbols | Consumers vs includes |
|------|---------|------------------------|
| `job_capabilities.h` | `kRoleSamplesCore1`, `kRoleRunsLogger` | `health_monitor.cpp` only (matches 1 include) |
| `job_capabilities.h` | `kRoleHasFullGoNogo` | **Zero consumers** (defined, never used) |
| `prearm_fail_ticks.h` | `kPreArmFailTicks`, `prearm_fail_tick_next` | `ao_notify.cpp` + `test_prearm_fail_ticks.cpp` (matches 2 includes) |
| `sensor_snapshot.h` | `SensorSnapshot` | **Only** `test/test_data_model.cpp` `static_assert` on sizeof. No firmware TU uses the type |
| `eskf_brake.cpp` | `eskf_note_divergence` | Declared in `eskf_runner.h`; called from `eskf_runner.cpp` + host test. Separate **TU** for host-test split, not a missing header |

### W-5 keep vs fold (recommendations only — no code this sitting)

Do **not** copy a fold from one thin header onto another.

| Recommendation | Paths | Why |
|----------------|-------|-----|
| **KEEP** | `job.h`, `led_patterns.h`, `vec3.h`, `mat.h` (rename is WN-074, not a fold), `rfm95w.h`, `ws2812_status.h`, calibration trio, `flight_director.h`, `action_executor.h`, `flight_table.h`, `crc32.h`, `sensor_core1.h`, `ao_radio.h`, `radio_config_table.h`, `anomalous_boot.h`, `core1_i2c_pause.h`, `data_convert.h`, `radio_config_storage.h`, `guard_*`, `diag_stats.h` | Fan-in ≥ 3 production includes and/or a real API |
| **KEEP the pair, ignore “folder for one pair”** | `core1/`, `station/idle_tick`, `diag/` | The *pair* has consumers; a folder-with-one-pair is naming/layout, not “delete the module” |
| **KEEP job packs as three files** | `job_vehicle.h` / `job_station.h` / `job_relay.h` | Selector-only includes (1 each) are **the point** of compile-time role packs, not evidence they should merge |
| **KEEP** `prearm_fail_ticks.h` | 1 production + 1 host test of a pure helper | That is the intended split (host-testable, no AO in the test) |
| **FOLD / park** `kRoleHasFullGoNogo` | dead constexpr in `job_capabilities.h` | Symbol consumer = 0. Folding the *file* is a different question (`health_monitor` still needs the other two) |
| **KEEP file, unused type** `sensor_snapshot.h` | firmware does not use `SensorSnapshot` | Either wire it into logging or treat as dead API when earn-rent is labeled — not a silent delete tonight |
| **KEEP TU** `eskf_brake.cpp` | host-test split is the written reason | 0 header includes is expected |
| **Not a header W-5** | `main.cpp`, `shared_state.cpp`, `flight_in_progress.cpp`, `notify_backend_audio.cpp` | TUs / stub; earn-rent WNs on these are “what is this file,” not include-count |

---

## W-2 — owner / mutator / barrier

Ambiguity **is** the finding. `volatile` is not a hardware barrier (`MULTICORE_RULES.md`).

### 22 `volatile`

| Object | Owner (who is allowed to write the contract) | Mutator(s) seen | Barrier | Ambiguity |
|--------|-----------------------------------------------|-----------------|---------|-----------|
| `rc_log.cpp` `g_ring/g_head/g_tail/g_droppedBytes/g_highWater` | log module; producer `rc_log`, consumer drain | Same TU: write path vs `tud_cdc_write` drain | `volatile` only | **Yes** if `rc_log` runs on Core 1 while drain is Core 0 USB. Same-core would be ISR vs thread — still only `volatile` |
| `gps_uart.cpp` `g_rxHead` | UART ISR | ISR write; `gps_uart_reinit`/`reset` zero | `volatile` | **Yes** — ISR vs Core 1 consumer; comment claims `volatile` enough |
| `gps_uart.cpp` `g_rxTail` | consumer (`gps_uart` poll) | consumer write; ISR reads for full | `volatile` | **Yes** — pair with `g_rxHead` |
| `gps_uart.cpp` `g_rxOverflow` | ISR | ISR increment; reinit zeros | `volatile` | Diagnostic; same ISR/thread split |
| `test_mode.cpp` `g_test_mode_arm_magic` | debugger / test_mode_init | GDB poke; init clears | `volatile` (debugger-visible SRAM) | Boot-window by design; not a Core1 handshake |
| `test_mode.cpp` `g_test_mode_enabled` | `test_mode.cpp` | init/poll/disable | `volatile` | Core 0 CLI/FD readers via accessor |
| `test_mode.cpp` `g_magicObservedAtBoot` | `test_mode_init` | init only | `volatile` | Same |
| `rc_os_commands.cpp` `g_t2_*` | station RX vs queue | `stage_t2_queue_command` vs `stage_t2_fire_pending_if_any` | `volatile` | **Gated** `#ifdef ROCKETCHIP_STAGE_T2_CHEAT` — dead unless that flag is on |
| `fault_inject.cpp` `g_fault_core0_stall` / `g_fault_watchdog_skip` | CLI inject vs `main` idle | CLI set; `main.cpp` read/decrement | `volatile` | Same-core if CLI and idle are Core 0; still no DMB |
| `station_fault_inject.cpp` `g_fault_station_rx_drop_remaining` / `g_fault_station_ack_suppress_remaining` | inject vs `ao_telemetry.cpp` | inject set; telemetry decrements | `volatile` | Station AO vs inject CLI — same core likely |
| `fault_protection.cpp` `g_inFaultHandler` | fault handlers | set true on entry | `volatile` | Recursion/reentry guard; same-core exception path |
| `flight_in_progress.cpp` `g_flightInProgressMagic` | this TU | set/clear in this TU | `volatile` | Persist-across-reset magic; not a live cross-core flag |
| `pyro_edge_logger.cpp` `g_count` | GPIO IRQ callback | IRQ increments; init/dump read | `volatile` | **Yes** — IRQ vs CLI dump; `g_buffer[]` is **not** volatile |
| `flight_director.cpp` `g_phaseObservablePair` | FD set; fault_protection get | `flight_phase_observable_set` (FD + fault path) / `get` | `volatile` packed word | Fault handler vs FD on Core 0; packing is the contract |

### 9 `std::atomic`

| Object | Owner | Mutator(s) | Barrier | Ambiguity |
|--------|-------|------------|---------|-----------|
| `g_startSensorPhase` | Core 0 `main` | `store(true)` after init | `memory_order_release` / Core 1 wait | Named handshake |
| `g_sensorPhaseDone` | **none — dead** | **No load, no store.** `extern` in `shared_state.h` and `sensor_seqlock.h`; definition `shared_state.cpp` init `false`. | none (never published) | **Yes — dead flag.** Dual home, zero producer, zero consumer. Handshake that actually runs is `g_startSensorPhase`. Delete vs document is a Phase 2/3 label, not a W-2 rewrite. |
| `g_calReloadPending` | cal hooks / Core 1 | Core 1 consume+clear | acquire/release | Pause header comments Core 0 `cal_post_hook` as a writer |
| `g_core1PauseI2C` / `g_core1I2CPaused` | `core1_i2c_pause.cpp` Core 0 vs Core 1 loop | pause/resume vs sensor_core1 | acquire/release | Nesting not in this pass (agent two-agree, later chunk) |
| `g_core1LockoutReady` | Core 1 | Core 0 waits in `main` | acquire | Boot lockout |
| `g_bestGpsValid` | Core 1 GPS path | Core 1 store; station_fault_inject also **stores false** | atomic | **Yes** — second writer in `station_fault_inject.cpp` |
| `rc_os_mag_cal_active` | AO_RCOS | `ao_rcos.cpp` store; Core 1 load | acquire/release | Clear owner |
| `g_spi_error_count` | SPI driver | `fetch_add` relaxed; diag load | relaxed atomic | Count only |

**Dual home:** the six Core0↔Core1 atomics are `extern` in both `shared_state.h` and `sensor_seqlock.h`. That is a packaging/SSOT fact for earn-rent, not a second object.

**Same test, not in the 22+9 count:** seqlock snapshot, PSRAM ring, AO static events — not walked this sitting (call out if Phase 2 wants them before safety/fusion labels).

---

## What this is for

Phase 2 (label WNs) should use these numbers when a keep/fold or concurrency WN comes up.

---

## Phase 1 close — 2026-08-21

Owner: Phase 0 paper holes skipped; finish Phase 1 before any Phase 2 labels.

Population (same as 2026-08-20): authored `src/` + `include/rocketchip/` + `test/`. **Direct** = `#include` of the basename. **Indirect** = files that reach the header only through another authored include (transitive), not counted in Direct. **Symbol consumers** on thin headers = files that mention a name *defined in that header*. Hub APIs (`flight_director.h`, `vec3.h`, …) are used; a mechanical identifier scrape over-counts those and is not published here.

`sensor_snapshot.h` is **gone from prod** (parked 2026-08-20, DF-001). `kRoleHasFullGoNogo` is **gone** (dead constexpr, same sitting). Direct counts that moved vs the 2026-08-20 table are called out.

### Contested headers — three numbers

| Path | Direct | Indirect | Symbol consumers / note |
|------|-------:|---------:|-------------------------|
| `job.h` | 9 | 10 | `DeviceRole` enum; `config.h` is the extra hub. Indirect includes CLI, `main`, health_monitor, rfm/spi, eskf_runner, pcm_frame, fault_inject |
| `job_capabilities.h` | 1 | 0 | `kRoleSamplesCore1`, `kRoleRunsLogger`: **`health_monitor.cpp` only**. Matches 1 include |
| `job_vehicle.h` / `job_station.h` / `job_relay.h` | 1 each | 19 each | Selector-only via `job.h`. Indirect fan-in **is the point** of the packs, not evidence to merge |
| `led_patterns.h` | 6 | 0 | Live pattern API. `kOff` enumerator has no users |
| `prearm_fail_ticks.h` | 2 | 0 | `ao_notify.cpp` + `test_prearm_fail_ticks.cpp`. Matches 2 includes |
| `vec3.h` | 16 | 26 | Real math API (via `eskf.h` / `quat.h` / mahony) |
| `mat.h` | 6 | 24 | Real math API. Rename is WN-074, not a fold |
| `rfm95w.h` | 4 | 7 | Extra 7 via `ao_radio.h` |
| `ws2812_status.h` | 5 | 0 | |
| `eskf_brake.h` | **n/a** | n/a | **No header.** Brake is `eskf_brake.cpp`; decl lives in `eskf_runner.h` |
| `calibration_data.h` | 5 | 8 | |
| `calibration_manager.h` | 9 | 0 | |
| `calibration_storage.h` | 4 | 0 | `rc_os_commands.cpp` includes; symbols used in manager / storage / `main` |
| `flight_director.h` | **18** (was 22 on 08-20) | 3 | Hub. Recount 2026-08-21 |
| `action_executor.h` | 5 | 19 | Extra 19 mostly via `flight_director.h` |
| `guard_combinator.h` | 3 | 19 | Same: pulled by `flight_director.h`. `kMaxCombinators` unused |
| `guard_functions.h` | 3 | 0 | |
| `flight_table.h` | 8 | 1 | `flash_flush.cpp` via `flash_flush.h`. `kFlightLogSize` unused |
| `data_convert.h` | 4 | 0 | |
| `radio_config_storage.h` | 3 | 0 | |
| `crc32.h` | 5 | 0 | |
| `diag_stats.h` | 3 | 0 | Pair in `src/diag/` |
| `station_idle_tick.h` | 2 | 0 | Pair in `src/station/` |
| `anomalous_boot.h` | 3 | 0 | |
| `core1_i2c_pause.h` | 3 | 0 | Direct: `ao_rcos.cpp`, `rc_os_commands.cpp`, own `.cpp`. Pause names also appear in cal_hooks / `rc_os` comments |
| `sensor_core1.h` | 9 | 0 | Pair in `src/core1/` |
| `ao_radio.h` | 9 | 0 | |
| `radio_config_table.h` | 4 | 0 | WB seed |
| `sensor_snapshot.h` | **0** (parked) | 0 | Type not in prod. Layout at `parked/sensor_snapshot.h` |

### Not headers (earn-rent WNs are “what is this file”)

`main.cpp`, `shared_state.cpp`, `flight_in_progress.cpp`, `notify_backend_audio.cpp`, `eskf_brake.cpp` — include-count does not apply.

### W-2 hole closed

`g_sensorPhaseDone`: confirmed unused (see table above). Seqlock snapshot, PSRAM ring, AO static events stay **out of** the 22+9 list (plan did not require them). Ambiguity rows already in the 2026-08-20 tables stand.

Keep/fold recommendations from the first sitting are unchanged. No further `src/` from Phase 1.
