# SDK submodule (not RocketChip source)
pico-sdk/

# Vendored / third-party "adopted code" — NOT first-party architecture.
# First-party thin wrappers live in src/ (e.g. src/telemetry/ for MAVLink).
# EXTERNAL = ETL header lib (~432 files); lib/ = vendored drivers (icm20948, lwgps, ruuvi, ap_compat, mavlink).
EXTERNAL/
lib/

# Build / generated
build*/
cmake-build-*/
cmake_test_discovery_*.json

# Large binaries / firmware artifacts
*.uf2
*.elf
*.bin
*.dis
*.map

# Scratch / agent temp
agent-tools/
tools/scratch/

# SPIN generated outputs
tools/spin/pan.*

# Curated out to match Grok pass-3 (cleaner architectural graph)
# Tests + tooling scripts:
test/
tests/
Testing/
scripts/
# CCSDS-detour leftovers + Starcom side-project + logs:
mcps/
terminals/
starcom/
logs/
# Historical / point-in-time docs — keep the graph a current-state map (prune churn):
docs/plans/
docs/audits/
docs/baselines/
CHANGELOG.md
# Agent / editor tooling config (not firmware architecture):
.claude/
.agents/
.cursor/
.vscode/
.github/

# graphify's own output — prevent self-ingestion / pollution + protect snapshots
graphify-out/
graphify-out-codeonly/
graphify-grok-pass3-BACKUP-2026-06-27/

# Images / papers / audio / video — deferred (skipping photos for now, 2026-06-27).
# Docs/standards markdown is now INGESTED (deferral block removed for the full run).
**/*.png
**/*.jpg
**/*.jpeg
**/*.webp
**/*.gif
**/*.svg
**/*.pdf
**/*.mp4
**/*.mov
**/*.wav
**/*.mp3

# Build script (not a doc)
CMakeLists.txt

# Build logs (not source)
GRAPHIFY_BUILD_*.log
